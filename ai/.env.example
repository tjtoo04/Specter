# =============================================================================
# Mystery Shopper – Alert configuration
# =============================================================================
# Copy this file to .env and set every variable. All variables are mandatory.
#
#   cp ai/.env.example ai/.env
#   # Edit ai/.env with your real values
# =============================================================================

# -----------------------------------------------------------------------------
# Slack 
# -----------------------------------------------------------------------------
# Bot User OAuth Token from https://api.slack.com/apps
# Create an app → OAuth & Permissions → Bot User OAuth Token
# Scopes: chat:write, chat:write.public. Invite the bot to your channels.
SLACK_BOT_TOKEN=xoxb......

# -----------------------------------------------------------------------------
# Microsoft Teams 
# -----------------------------------------------------------------------------
# Incoming webhook URL from a Teams channel
# Channel → … → Connectors → Incoming Webhook → Configure
TEAMS_WEBHOOK_URL=https://outlook.office.com/webhook/...

# -----------------------------------------------------------------------------
# Discord 
# -----------------------------------------------------------------------------
# Webhook URL from Discord: Channel Settings → Integrations → Webhooks → New
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/...

# -----------------------------------------------------------------------------
# Jira 
# -----------------------------------------------------------------------------
# Webhook or automation endpoint that accepts POST JSON.
# See ai/docs/JIRA_WEBHOOK_SETUP.md for step-by-step (Jira Automation, Zapier, Make).
JIRA_WEBHOOK_URL=https://your-domain.atlassian.net/...

# Optional: for Jira Automation, paste the trigger secret to send as X-Automation-Webhook-Token.
# If empty, append the secret to the URL instead (see docs).
# JIRA_WEBHOOK_SECRET=

# -----------------------------------------------------------------------------
# Routing Agent – Gemini 
# -----------------------------------------------------------------------------
# Get key: https://aistudio.google.com/apikey
GEMINI_API_KEY=your-gemini-api-key
# Optional; default gemini-2.5-flash. If quota issues, try: gemini-2.5-flash-lite or gemini-3-flash
# GEMINI_MODEL=gemini-2.5-flash
